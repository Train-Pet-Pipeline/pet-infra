# OmegaConf interpolation below is resolved when params.yaml is merged into
# the stage config context by the orchestrator (deferred to a later PR).
# Until then this remains a literal string when loaded standalone.
storage_root: ${params.ota.local_backend.storage_root}
