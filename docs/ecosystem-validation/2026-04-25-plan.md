# 2026-04-25 生态实战验证 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在租用 RTX 5090 级 GPU 上对 Train-Pet-Pipeline 9 仓生态执行端到端实战验证，按 4 阶段（Bootstrap → Critical Path → Capability Matrix → Cross-Cutting）覆盖所有宣称用法，零绕过 find-fix-retest，所有发现写入 living report。

**Architecture:** 计划分三部分：**Part A** 是租卡前的本地准备工作（脚本 / 配置 / known-unknown 解决）；**Part B** 是租卡上的 4-Phase 执行 playbook，每个 Phase 是一组 step + 通过判据；**Part C** 是租卡后的收尾（findings 聚合、retro、批量 PR）。Plan 设计为可被 `superpowers:subagent-driven-development` 拾起执行，但 Part B 在 GPU 实战环境下，我（Claude）按 spec §4.6 决策合成自主推进。

**Tech Stack:** Python 3.11 / pip-compile / conda env `pet-pipeline` / mmengine plugin registry / pet-infra orchestrator / Hydra / pytest / SQLite + 自定义 migration / OpenAI-compat client → Anthropic API / huggingface-cli / git PAT / cron / ClearML offline.

**Spec 锚点：** [`2026-04-25-design.md`](2026-04-25-design.md)（第 2 轮 critic APPROVED-WITH-FIXES + 用户 OK）。

---

## 文件结构总览

本次活动产出的文件（不含 spec / plan 自身）：

```
pet-infra/docs/ecosystem-validation/
├── README.md                                 # 索引（已建）
├── 2026-04-25-design.md                      # 设计 spec（已建）
├── 2026-04-25-plan.md                        # 本文件
├── 2026-04-25-report.md                      # rental 实时 living doc（Part A4 创建 skeleton）
├── 2026-04-25-findings/
│   └── F###-<slug>.md                        # 每个 finding 一文件（Part B 累积）
└── 2026-04-25-artifacts/
    ├── data-subset-manifest.json             # Part A2 产出
    ├── select_data_subset.py                 # Part A2 产出
    ├── bootstrap_rental.sh                   # Part A3 产出
    ├── kickoff.md                            # Part A5 产出
    ├── pet-train-sft-loss.png                # Part B Phase 1 产出
    ├── pet-eval-vision-report.json           # Part B Phase 1 产出
    ├── pet-eval-fusion-3plugin-reports/      # Part B Phase 2 E3 产出
    └── ...
```

修改的代码（如 fix）走各自仓库的 `feature/*` 分支，**不**在 pet-infra 里。

---

# Part A — 租卡前本地准备

> **Part A 在当前会话完成（不需要 GPU）**。每个 task 走 TDD 模式：写测 → 实现 → 验证 → 提交。
> 全部 commit 到 `feature/eco-validation-design-2026-04-25` 分支（已存在）。

## Task A1: 解决附录 A 剩余 known unknowns

**目的**：spec §附录 A 列了 3 项需 plan 阶段 verify 的问题（D4 fps 默认值、Sonnet endpoint JSON mode、tiny_test 默认 device）。本 task 把它们查清，结果写进 `data-subset-manifest.json` 或更新 spec 附录 A。

**Files:**
- Read: `pet-data/configs/**/*.yaml`（找 frame_extract fps）
- Read: `pet-train/src/pet_train/plugins/tiny_test*.py`（找默认 device）
- Read: Anthropic OpenAI-compat docs（验证 JSON mode 是否支持）
- Modify: `docs/ecosystem-validation/2026-04-25-design.md` 附录 A（如有更新）

- [ ] **Step 1: 查 pet-data video frame_extract fps 默认值**

```bash
grep -rn "fps\|frame_rate\|frames_per_second" /Users/bamboo/Githubs/Train-Pet-Pipeline/pet-data/configs/ /Users/bamboo/Githubs/Train-Pet-Pipeline/pet-data/src/pet_data/sources/youtube.py 2>/dev/null | head -20
```

Expected: 找到一个数值（如 `fps: 1.0` 或 `fps: 5`）。

- [ ] **Step 2: 查 pet-train tiny_test plugin 默认 device**

```bash
grep -n "device\|cpu\|cuda" /Users/bamboo/Githubs/Train-Pet-Pipeline/pet-train/src/pet_train/plugins/_register.py /Users/bamboo/Githubs/Train-Pet-Pipeline/pet-train/src/pet_train/trainers/tiny*.py 2>/dev/null
```

Expected: 看到 device 默认值（应为 `cpu` 或 `auto`）。

- [ ] **Step 3: 验证 Anthropic OpenAI-compat endpoint 是否支持 `response_format=json_object`**

调 `document-specialist` agent 短查 Anthropic 官方文档：
- endpoint: `https://api.anthropic.com/v1/`
- 是否支持 OpenAI-style `response_format: {"type": "json_object"}`
- 备选方案：如不支持，pet-annotation 改 prompt-based JSON（已是默认行为）

- [ ] **Step 4: 把发现写进 spec 附录 A 并更新（如必要）**

如发现影响 design：用 Edit 工具更新 `docs/ecosystem-validation/2026-04-25-design.md` 附录 A 表格。

- [ ] **Step 5: Commit**

```bash
cd /Users/bamboo/Githubs/Train-Pet-Pipeline/pet-infra
git add docs/ecosystem-validation/2026-04-25-design.md
git commit -m "docs(pet-infra): plan A1 — resolve appendix A known unknowns

- pet-data video fps default: <填值>
- pet-train tiny_test default device: <填值>
- Anthropic OAI-compat JSON mode support: <填值>

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task A2: 写 data subset selection script + 生成 manifest

**目的**：把 spec §1.5 的"50 样本，确定性选样"实现为脚本，输出 `data-subset-manifest.json`（含每文件 sha256），rental 上一键复现。

**Files:**
- Create: `docs/ecosystem-validation/2026-04-25-artifacts/select_data_subset.py`
- Create: `docs/ecosystem-validation/2026-04-25-artifacts/data-subset-manifest.json`
- Test: 运行脚本，肉眼 verify manifest 内容

- [ ] **Step 1: 写 selection script**

```python
# docs/ecosystem-validation/2026-04-25-artifacts/select_data_subset.py
"""按 spec §1.5 确定性选 50 样本（cat 25 / dog 25），写 manifest。

选样规则：
- 图片 / 音频：字典序前 N
- 视频：duration 升序，duration 相同则字典序升序，取前 N
"""
from __future__ import annotations

import hashlib
import json
import subprocess
from pathlib import Path

import os
DATA_ROOT = Path(os.environ.get("PET_DATA_ROOT", "/Users/bamboo/Githubs/Purr-Sight/data"))
OUTPUT = Path(__file__).parent / "data-subset-manifest.json"

PLAN = [
    ("cat_images", 15, "lex"),
    ("dog_images", 15, "lex"),
    ("cat_audio", 8, "lex"),
    ("dog_audio", 8, "lex"),
    ("cat_video", 2, "duration"),
    ("dog_video", 2, "duration"),
]


def sha256_of(path: Path) -> str:
    h = hashlib.sha256()
    with path.open("rb") as f:
        for chunk in iter(lambda: f.read(65536), b""):
            h.update(chunk)
    return h.hexdigest()


def video_duration(path: Path) -> float:
    """ffprobe 读时长；失败返回 inf 让该文件排到最后。"""
    try:
        result = subprocess.run(
            [
                "ffprobe", "-v", "error", "-show_entries", "format=duration",
                "-of", "default=noprint_wrappers=1:nokey=1", str(path),
            ],
            capture_output=True, text=True, timeout=10, check=True,
        )
        return float(result.stdout.strip())
    except (subprocess.CalledProcessError, ValueError, subprocess.TimeoutExpired):
        return float("inf")


def select(category: str, n: int, sort_mode: str) -> list[Path]:
    folder = DATA_ROOT / category
    files = sorted(p for p in folder.iterdir() if p.is_file())
    if sort_mode == "lex":
        return files[:n]
    if sort_mode == "duration":
        # tuple sort: (duration, name) 升序
        return sorted(files, key=lambda p: (video_duration(p), p.name))[:n]
    raise ValueError(f"unknown sort_mode: {sort_mode}")


def main() -> None:
    manifest = {"spec": "2026-04-25-design.md §1.5", "samples": []}
    for category, n, mode in PLAN:
        for p in select(category, n, mode):
            manifest["samples"].append({
                "category": category,
                "filename": p.name,
                "size_bytes": p.stat().st_size,
                "sha256": sha256_of(p),
                "abs_path_local": str(p),
            })
    OUTPUT.write_text(json.dumps(manifest, indent=2, ensure_ascii=False))
    print(f"wrote {len(manifest['samples'])} entries → {OUTPUT}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 运行脚本生成 manifest**

```bash
cd /Users/bamboo/Githubs/Train-Pet-Pipeline/pet-infra
python docs/ecosystem-validation/2026-04-25-artifacts/select_data_subset.py
```

Expected: `wrote 50 entries → ...data-subset-manifest.json`

- [ ] **Step 3: 验证 manifest 完整性**

```bash
python -c "
import json
m = json.load(open('docs/ecosystem-validation/2026-04-25-artifacts/data-subset-manifest.json'))
assert len(m['samples']) == 50, len(m['samples'])
from collections import Counter
c = Counter(s['category'] for s in m['samples'])
print(c)
assert c['cat_images'] == 15 and c['dog_images'] == 15
assert c['cat_audio'] == 8 and c['dog_audio'] == 8
assert c['cat_video'] == 2 and c['dog_video'] == 2
print('OK')
"
```

Expected: counter 输出 + `OK`

- [ ] **Step 4: Commit**

```bash
git add docs/ecosystem-validation/2026-04-25-artifacts/select_data_subset.py \
        docs/ecosystem-validation/2026-04-25-artifacts/data-subset-manifest.json
git commit -m "feat(pet-infra): plan A2 — data subset selector + manifest

50 samples, species 25/25, modality 30/16/4, deterministic selection
per spec §1.5 (lex / duration+lex tie-break).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task A3: 写 bootstrap_rental.sh 脚本

**目的**：把 spec §2.1 Phase 0 的 12 个 step（0.0-0.8）实现为一个 idempotent shell 脚本，rental 上一键执行。脚本失败立即 exit + 打印失败 step ID。

**Files:**
- Create: `docs/ecosystem-validation/2026-04-25-artifacts/bootstrap_rental.sh`

- [ ] **Step 1: 写脚本骨架**

```bash
#!/usr/bin/env bash
# docs/ecosystem-validation/2026-04-25-artifacts/bootstrap_rental.sh
# Spec: pet-infra/docs/ecosystem-validation/2026-04-25-design.md §2.1
# Idempotent: 可重复运行；失败立即 exit 1 + step ID

set -euo pipefail

WORKSPACE="${WORKSPACE:-/workspace}"
MATRIX_TAG="2026.10-ecosystem-cleanup"
ORG="https://github.com/Train-Pet-Pipeline"
REPOS=(pet-schema pet-infra pet-data pet-annotation pet-train pet-eval pet-quantize pet-ota pet-id)

log() { printf '[bootstrap] %s\n' "$*" >&2; }
fail() { log "FAIL at step $1: $2"; exit 1; }

mkdir -p "$WORKSPACE"
cd "$WORKSPACE"

# 0.0 — 环境基线
log "step 0.0 — env baseline"
df -h . | awk 'NR==2 {gsub(/G/, "", $4); if ($4+0 < 150) exit 1}' \
  || fail "0.0" "disk free < 150GB"
nvidia-smi >/dev/null || fail "0.0" "nvidia-smi failed"
python -c "import sys; assert sys.version_info[:2]==(3,11)" \
  || fail "0.0" "python is not 3.11"
[[ -n "${GH_TOKEN:-}" ]] || fail "0.0" "GH_TOKEN not set"
git config --global credential.helper store
echo "https://x-access-token:${GH_TOKEN}@github.com" > ~/.git-credentials
export HF_HOME="${HF_HOME:-$WORKSPACE/hf-cache}"
mkdir -p "$HF_HOME/qwen2vl2b" "$HF_HOME/panns"

# 0.1 — clone 9 repos at matrix tag
log "step 0.1 — clone 9 repos at $MATRIX_TAG"
# matrix tag 是 pet-infra 上的，其他仓 tag 名见 compatibility_matrix.yaml
declare -A REPO_TAGS=(
  [pet-schema]="v3.2.1"
  [pet-infra]="v2.6.0"
  [pet-data]="v1.3.0"
  [pet-annotation]="v2.1.1"
  [pet-train]="v2.0.2"
  [pet-eval]="v2.3.0"
  [pet-quantize]="v2.1.0"
  [pet-ota]="v2.2.0"
  [pet-id]="v0.2.0"
)
for repo in "${REPOS[@]}"; do
  if [[ ! -d "$repo/.git" ]]; then
    git clone "$ORG/$repo.git" "$repo" || fail "0.1" "clone $repo"
    git -C "$repo" checkout "${REPO_TAGS[$repo]}" || fail "0.1" "checkout ${REPO_TAGS[$repo]} on $repo"
  fi
done

# 0.2 — conda + CUDA torch
log "step 0.2 — verify conda env + CUDA torch"
source "$(conda info --base)/etc/profile.d/conda.sh"
# 创建 env 如不存在（fresh rental）
conda env list | grep -q "^pet-pipeline " || \
  conda create -n pet-pipeline python=3.11 -y
conda activate pet-pipeline
# 装 CUDA torch 如未装（fresh rental）
python -c "import torch" 2>/dev/null || \
  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
python -c "import torch; assert torch.cuda.is_available()" \
  || fail "0.2" "torch.cuda not available (check CUDA driver vs torch wheel)"

# 0.3 — pet-infra 3-step
log "step 0.3 — pet-infra setup (3-step)"
pip install "pet-schema @ git+$ORG/pet-schema.git@${REPO_TAGS[pet-schema]}" \
  || fail "0.3" "pet-schema install"
(cd pet-infra && pip install -e ".[dev,api,sync]") || fail "0.3" "pet-infra install"
python -c "import pet_schema, pet_infra; print(pet_schema.__version__, pet_infra.__version__)" \
  || fail "0.3" "version assert"

# 0.4 — 7 仓 setup
log "step 0.4 — 7 downstream repos setup"
export PET_ALLOW_MISSING_SDK=1
for repo in pet-data pet-annotation pet-train pet-quantize; do
  (cd "$repo" && pip install -e ".[dev]") || fail "0.4" "$repo install"
done
# pet-eval 走 4-step
(cd pet-eval && pip install -e ".[dev]") || fail "0.4" "pet-eval install"
(cd pet-ota && pip install -e ".[dev]") || fail "0.4" "pet-ota install"
(cd pet-id && pip install -e ".[dev]") || fail "0.4" "pet-id install"
python -c "
import pet_data, pet_annotation, pet_train, pet_eval, pet_quantize, pet_ota, pet_id
print('all 7 import OK')
" || fail "0.4" "import check"

# 0.4.5 — DB init（实际类是 FrameStore + AnnotationStore；DB 文件按惯例 frames.db）
log "step 0.4.5 — DB init"
mkdir -p "$WORKSPACE/raw_data"
python -c "
from pathlib import Path
from pet_data.storage.store import FrameStore
FrameStore(Path('$WORKSPACE/raw_data/frames.db'))
print('frames.db init OK')
" || fail "0.4.5" "pet-data FrameStore init"
python -c "
from pet_annotation.store import AnnotationStore
s = AnnotationStore('$WORKSPACE/annotation.db')
s.init_schema()
print('annotation.db init OK')
" || fail "0.4.5" "pet-annotation AnnotationStore init"

# 0.4.6 — API key check
log "step 0.4.6 — API key check"
[[ -n "${ANTHROPIC_API_KEY:-}" ]] || fail "0.4.6" "ANTHROPIC_API_KEY not set"
log "ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:0:7}..."

# 0.4.7 — HF model preload
log "step 0.4.7 — HF model preload"
huggingface-cli download Qwen/Qwen2-VL-2B-Instruct \
  --local-dir "$HF_HOME/qwen2vl2b" --local-dir-use-symlinks False \
  || fail "0.4.7" "Qwen2-VL-2B download"
PANNS_PATH="$HF_HOME/panns/MobileNetV2.pth"
if [[ ! -f "$PANNS_PATH" ]]; then
  curl -L -o "$PANNS_PATH" \
    "https://zenodo.org/record/3987831/files/MobileNetV2_mAP%3D0.383.pth" \
    || fail "0.4.7" "PANNs MobileNetV2 download"
fi

# 0.4.8 — auto-commit cron
log "step 0.4.8 — auto-commit cron"
CRON_LINE='*/15 * * * * cd '"$WORKSPACE"'/pet-infra && git diff --quiet docs/ecosystem-validation/ || (git add docs/ecosystem-validation/ && git commit -m "wip eco-validation" && git push)'
( crontab -l 2>/dev/null | grep -v 'eco-validation' ; echo "$CRON_LINE" ) | crontab -

# 0.5 — make test 全 9 仓
log "step 0.5 — make test on 9 repos"
for repo in "${REPOS[@]}"; do
  log "  testing $repo"
  (cd "$repo" && make test) || fail "0.5" "make test on $repo"
done

# 0.6 — cross-repo smoke install
log "step 0.6 — cross-repo smoke install"
gh workflow run cross-repo-smoke-install.yml --repo Train-Pet-Pipeline/pet-infra \
  || log "0.6 WARN: gh workflow run failed, falling back to local replay"
# (本地 replay 逻辑 TBD on rental; 不 fail 整个 bootstrap)

# 0.7 — no-wandb-residue（修：原版 set -e + || true 互锁，导致 fail 永远不触发）
log "step 0.7 — no-wandb-residue check on 5 repos"
for repo in pet-train pet-eval pet-quantize pet-ota pet-annotation; do
  residue=$(cd "$repo" && grep -rln "wandb" src/ tests/ 2>/dev/null | grep -vE 'no_wandb|wandb-residue|guard' || true)
  if [[ -n "$residue" ]]; then
    fail "0.7" "wandb residue in $repo: $residue"
  fi
done

# 0.8 — done
log "bootstrap COMPLETE — Phase 0 ready for execution"
```

- [ ] **Step 2: shellcheck + chmod**

```bash
shellcheck docs/ecosystem-validation/2026-04-25-artifacts/bootstrap_rental.sh \
  || true   # warnings OK
chmod +x docs/ecosystem-validation/2026-04-25-artifacts/bootstrap_rental.sh
```

Expected: shellcheck 警告或干净；脚本 executable。

- [ ] **Step 3: dry-run 检查（不实际执行 install）**

```bash
bash -n docs/ecosystem-validation/2026-04-25-artifacts/bootstrap_rental.sh
```

Expected: 无语法错误。

- [ ] **Step 4: Commit**

```bash
git add docs/ecosystem-validation/2026-04-25-artifacts/bootstrap_rental.sh
git commit -m "feat(pet-infra): plan A3 — bootstrap_rental.sh for Phase 0

Idempotent shell script implementing spec §2.1 steps 0.0-0.8.
Fail-fast with step ID; pre-pulls Qwen2-VL-2B + PANNs weights;
sets up auto-commit cron.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task A4: 写 report.md skeleton

**目的**：按 spec §5.1 的固定章节，预先建好 report skeleton 框架，rental 上各 phase 完成后填段落。

**Files:**
- Create: `docs/ecosystem-validation/2026-04-25-report.md`

- [ ] **Step 1: 写 skeleton 文件**

按 spec §5.1 的 11 节结构，每节预占位。具体内容：见 spec §5.1 模板，照抄 + 标 `<!-- TBD: 在 Phase X 末填 -->`。

- [ ] **Step 2: 同时写 finding 模板（占位 F000）**

```bash
cat > docs/ecosystem-validation/2026-04-25-findings/F000-template.md <<'EOF'
# F000 — <模板，不计入正式 finding 列表>

| | |
|---|---|
| 发现时间 | YYYY-MM-DD HH:MM |
| 发现 phase | Phase X / 仓库 / 步骤 |
| severity | STRUCTURAL / CRITICAL / HIGH / MEDIUM / LOW |
| 状态 | OPEN / FIXED / DEFERRED |
| 北极星受影响维度 | Pluggability / Flexibility / Extensibility / Comparability |

## 复现命令

```bash
<command>
```

## 实际行为

（log 摘录）

## 期望行为

（依据 architecture.md / DEV_GUIDE 引用）

## 根因

（systematic-debugging 输出，证据链）

## 修复

- commit: `<hash>`
- PR: `<url>`（B 路径未开 PR 时填 "B-batch"）
- 涉及文件：

## Retest 证据

- 命令：
- 输出：
- 同 phase 其它已 pass step 的 smoke retest：✅

## Follow-ups（如有）
EOF
```

- [ ] **Step 3: Commit**

```bash
git add docs/ecosystem-validation/2026-04-25-report.md \
        docs/ecosystem-validation/2026-04-25-findings/F000-template.md
git commit -m "feat(pet-infra): plan A4 — report skeleton + finding template

Per spec §5.1/§5.2.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task A5: 写 rental kickoff 命令 sheet

**目的**：rental 启动时，我（或后续接手的人）按这份 sheet 一键开干，不需要重新读 spec。

**Files:**
- Create: `docs/ecosystem-validation/2026-04-25-artifacts/kickoff.md`

- [ ] **Step 1: 写 kickoff.md**

```markdown
# Rental Kickoff Cheatsheet — 2026-04-25

## 0. SSH 进 rental + 准备 env vars

```bash
export GH_TOKEN=<github-pat>
export ANTHROPIC_API_KEY=<anthropic-key>
export WORKSPACE=/workspace
export HF_HOME=$WORKSPACE/hf-cache
```

## 1. 拉 pet-infra（拿到本 plan + bootstrap script）

```bash
mkdir -p $WORKSPACE && cd $WORKSPACE
git clone https://x-access-token:${GH_TOKEN}@github.com/Train-Pet-Pipeline/pet-infra.git
cd pet-infra
git checkout feature/eco-validation-design-2026-04-25
```

## 2. 跑 bootstrap

```bash
bash docs/ecosystem-validation/2026-04-25-artifacts/bootstrap_rental.sh
```

完成后预期：所有 9 仓 import OK + DB init + HF 模型下载 + cron 装好。

## 3. 选数据子集到 rental（用本地 manifest 复制 50 文件）

⚠️ rental 上不应再有 `/Users/bamboo/...` 路径。改 `select_data_subset.py` 的 `DATA_ROOT` 或单独写一个 `download_subset_from_manifest.py`，从 manifest 的 sha256 + filename 校验本地副本。

> Plan-time TODO: rental 上需要怎么拿到 50 个原始文件？三种选项：
> a) `scp` 本地 50 文件到 rental（小，~50MB，几分钟）
> b) 把 50 文件单独打 tar.gz 推到 GH releases（半公开，需注意版权）
> c) Purr-Sight repo 已 public 时直接 git clone Purr-Sight 然后 select
>
> **决策（spec §4.6）**：选 (a)，本地 scp。理由：测试精神 0 / 北极星 0 / 生态 0 / 工程 +1（最简单）。kickoff 上记录 scp 命令。

```bash
# 本地操作（rental 启动后从 laptop 跑）：
scp -r /Users/bamboo/Githubs/Purr-Sight/data \
  user@rental-ip:/workspace/raw_data/
# rental 上 verify：
python -c "
import json, hashlib
from pathlib import Path
m = json.load(open('docs/ecosystem-validation/2026-04-25-artifacts/data-subset-manifest.json'))
root = Path('/workspace/raw_data')
for s in m['samples']:
    p = root / s['category'] / s['filename']
    assert p.exists(), p
    h = hashlib.sha256(p.read_bytes()).hexdigest()
    assert h == s['sha256'], (p, h, s['sha256'])
print(f\"verified {len(m['samples'])} samples sha256 match\")
"
```

## 4. 启动 Phase 1 — Critical Path E2E

按本 plan Part B Phase 1 步骤 1.1 → 1.8 顺序执行。每步完成 commit + push。

## 5. 持续追加 report.md

每完成一段，立即 append 到 `docs/ecosystem-validation/2026-04-25-report.md`；cron 每 15 分钟自动 push。

## 6. 收尾

按本 plan Part C 走。
```

- [ ] **Step 2: Commit**

```bash
git add docs/ecosystem-validation/2026-04-25-artifacts/kickoff.md
git commit -m "feat(pet-infra): plan A5 — rental kickoff cheatsheet

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## Task A6: Part A 最终自检 + push 分支

- [ ] **Step 1: 检查分支文件齐全**

```bash
ls -la docs/ecosystem-validation/2026-04-25-artifacts/
```

Expected: `bootstrap_rental.sh` (executable), `select_data_subset.py`, `data-subset-manifest.json`, `kickoff.md` 都在。

- [ ] **Step 2: lint Python 脚本**

```bash
ruff check docs/ecosystem-validation/2026-04-25-artifacts/*.py
mypy --ignore-missing-imports docs/ecosystem-validation/2026-04-25-artifacts/*.py 2>&1 | tail -5
```

Expected: 干净或可接受 warning。

- [ ] **Step 3: push 分支到远端**

```bash
git push -u origin feature/eco-validation-design-2026-04-25
```

Expected: 分支推上去；返回分支 URL。

- [ ] **Step 4: 通知用户 Part A 完成 + 准备租卡**

输出给用户：
- 分支 URL
- 总 commit 数
- Part A 产出文件清单
- 下一步：用户租卡 + 给我 rental 信息（IP / SSH / GH_TOKEN / ANTHROPIC_API_KEY）
- 我会自主推 Part B + Part C

---

# Part B — Rental 执行 Playbook

> **Part B 在租卡上执行，由我（Claude Opus 4.7）按 spec §4.6 决策合成自主推进**。
> 用户授权：rental 期间全程自主，仅在 spec §4.7 触发条件停下问。
> 每步完成立即 append report.md + commit；cron 每 15 分钟 auto-push。

## Phase 0 — Bootstrap

- [ ] **B0.1: 跑 bootstrap_rental.sh**

```bash
bash docs/ecosystem-validation/2026-04-25-artifacts/bootstrap_rental.sh 2>&1 | tee /tmp/bootstrap.log
```

Pass: 退出码 0 + 最后一行 `bootstrap COMPLETE`。
Fail: 任何 `FAIL at step X.Y` → 立即停 → 写 finding `F00X-bootstrap-step-XY.md` → 30min 内不能修则 fail-fast 退租。

- [ ] **B0.2: scp 数据子集 + verify sha256**（按 kickoff.md §3）

- [ ] **B0.3: append Phase 0 段落到 report.md**

模板见 spec §5.1。

- [ ] **B0.4: commit + push**

```bash
git add docs/ecosystem-validation/
git commit -m "phase0(eco-validation): bootstrap complete on rental"
git push
```

## Phase 1 — Critical Path E2E

> 8 步顺序执行；每步完成 commit + push。

- [ ] **B1.1: pet-data ingest 30 张图（local_dir source）**

> pet-data CLI 是 params.yaml 驱动（不是 flag 驱动）。先准备一个临时 params。

```bash
cd /workspace/pet-data
# 写一个 ecosystem-validation 用的 params.yaml（覆盖 data_root + DB 路径）
cat > /workspace/eco-params.yaml <<EOF
data_root: /workspace/raw_data
sample:
  default_modality: vision
  storage_scheme: local
frames:
  extract_fps: 1.0
  dedup_hamming_threshold: 10
  quality_blur_threshold: 100.0
EOF

# ingest（local_dir source 会扫描 data_root 下所有图）
python -m pet_data.cli ingest --source local_dir --params /workspace/eco-params.yaml
# dedup
python -m pet_data.cli dedup --params /workspace/eco-params.yaml

# verify（注意类是 FrameStore，方法是 count_by_source）
python -c "
from pathlib import Path
from pet_data.storage.store import FrameStore
s = FrameStore(Path('/workspace/raw_data/frames.db'))
counts = s.count_by_source()
total = sum(counts.values())
assert total == 30, (counts, total)
print(f'OK ingested {total} frames, by source: {counts}')
"
```

Pass: total=30 + dedup 报告生成。
Finding 触发条件：count != 30 / dedup 缺失 / FrameStore API 报错。
Risk refs: R8（数据子集太小）、R16（DB init 已在 0.4.5 处理）。

- [ ] **B1.2: pet-annotation Sonnet label 30 张图（OpenAICompatProvider）**

> pet-annotation CLI 同样 params.yaml 驱动；provider/model/api_key 配在 params 的 llm 段。

```bash
cd /workspace/pet-annotation
# 写 ecosystem-validation 用的 params.yaml
cat > /workspace/eco-annotation-params.yaml <<EOF
database:
  path: /workspace/annotation.db
annotation:
  pet_data_db_path: /workspace/raw_data/frames.db
  batch_size: 5
llm:
  annotators:
    - name: sonnet_via_oai_compat
      provider: openai_compat
      base_url: https://api.anthropic.com/v1/
      model: claude-3-5-sonnet-20241022
      api_key_env: ANTHROPIC_API_KEY
classifier:
  annotators: []
rule:
  annotators: []
human:
  annotators: []
EOF
# ⚠️ 上述 yaml 字段需对照 pet_annotation/config.py 真实字段名 verify；
#    rental Phase 0 末做一次 `python -c "from pet_annotation.config import load_config; load_config(Path('/workspace/eco-annotation-params.yaml'))"`
#    报字段不对就按报错改。

# 跑 annotation（dispatch llm 范式）
python -m pet_annotation.cli annotate \
  --annotator llm \
  --modality vision \
  --params /workspace/eco-annotation-params.yaml \
  --db /workspace/annotation.db \
  --pet-data-db /workspace/raw_data/frames.db
# 导出 SFT + DPO JSONL（用真实 export subcommand）
python -m pet_annotation.cli export \
  --format sft --annotator llm \
  --params /workspace/eco-annotation-params.yaml \
  --db /workspace/annotation.db \
  -o /workspace/sft_v1.jsonl
python -m pet_annotation.cli export \
  --format dpo --annotator llm \
  --params /workspace/eco-annotation-params.yaml \
  --db /workspace/annotation.db \
  -o /workspace/dpo_v1.jsonl

# validate（用 pet-schema 真实 model）
python -c "
from pet_schema import ShareGPTSFTSample, DPOSample
sft_count = 0
with open('/workspace/sft_v1.jsonl') as f:
    for line in f:
        ShareGPTSFTSample.model_validate_json(line)
        sft_count += 1
dpo_count = 0
with open('/workspace/dpo_v1.jsonl') as f:
    for line in f:
        DPOSample.model_validate_json(line)
        dpo_count += 1
assert sft_count == 30, sft_count
assert dpo_count >= 1, dpo_count  # exporter 决定 pairs 数，可能少于 5
print(f'OK SFT={sft_count}, DPO={dpo_count}')
"
```

Pass: 30 SFT validate 通过 + DPO ≥ 1 条。

> **决策点**：如 Sonnet 返回非合法 JSON / 超时 / 配额 → spec §4.6 候选：
> - A: retry（pet-annotation 应已内置 tenacity）
> - B: 降级 Doubao（改 params.yaml provider 段）
> - C: 跳过 → 否决（违反 zero-bypass）
> 默认 A → 如 3 次失败切 B + 写 finding 标 `MEDIUM` "Anthropic compat 不稳定"。

- [ ] **B1.3: pet-train Qwen2-VL-2B SFT + LoRA — 复用 `recipes/smoke_small.yaml`**

> ⚠️ 关键发现：`recipes/smoke_small.yaml` 已实现 Qwen2-VL-2B LoRA SFT 全链（trainer + evaluator + converter + ota）。Phase 1 B1.3-B1.6 应**复用 smoke_small.yaml 而非编新 recipe**。但其 dataset 引用 `pet_annotation.vision_annotations`（DATASETS plugin 名），需我们将 sft_v1.jsonl 注册或换路径。

```bash
cd /workspace/pet-infra

# 写一个 ecosystem-validation 用的覆盖配置
mkdir -p configs/eco
cat > configs/eco/sft_train.yaml <<EOF
base_model: ${HF_HOME}/qwen2vl2b
lora_r: 16
lora_alpha: 32
lr: 1.0e-4
batch_size: 1
grad_accum: 4
max_steps: 30   # 极少步数，验证管线非验证模型
dataset: /workspace/sft_v1.jsonl   # 直接路径；如 trainer 不接受路径需改 plugin（写 finding）
output_dir: /workspace/runs/vision-sft
precision: fp16
device: cuda
EOF

# 跑 SFT — Hydra override 把 small_train.yaml 替换为 eco/sft_train.yaml
# 注：smoke_small 的 stages.train.config_path 引用 configs/smoke/small_train.yaml；
#    通过 Hydra override 改 config_path 即可
pet run recipes/smoke_small.yaml \
  stages.train.config_path=configs/eco/sft_train.yaml \
  2>&1 | tee /tmp/sft.log

# verify ModelCard
python -c "
import yaml
from pathlib import Path
mc_path = next(Path('/workspace/runs/vision-sft').glob('modelcard*.yaml'))
mc = yaml.safe_load(mc_path.read_text())
required = {'checkpoint_sha', 'git_sha', 'recipe_sha'}
missing = required - set(mc.keys())
assert not missing, missing
print(f'OK ModelCard at {mc_path}')
"

# loss 趋势
grep -E 'loss.*[0-9]' /tmp/sft.log | tail -5
```

Pass: ModelCard 三 sha 齐 + loss 在动（不要求收敛）。

> **决策点**：
> - 如 OOM → 降 batch_size / 减 max_steps（按 spec §4.6 决策合成）
> - 如 loss 完全不动 → MUST FIX（dataloader / collator bug）
> - 如 dataset 路径不接受要 plugin 名 → 改用 noop dataset 或注册临时 plugin（写 finding）
>
> Risk refs: R1（torch sm_120）、R2（transformers API drift）、R5（LoRA compat）

- [ ] **B1.4: pet-eval vision evaluator on 5 holdout**

> smoke_small.yaml 的 eval_fp stage 已经在 B1.3 跑过（依赖 train→eval_fp 链）。这里 verify 它的 report 即可。如要单独跑一次 eval（不重训），用 `--replay` + `stages=[eval_fp]` override 或读 modelcard。

```bash
cd /workspace/pet-infra
# verify smoke_small 的 eval_fp stage 已生成 report
EVAL_REPORT=$(find /workspace/runs/vision-sft -name "*eval*.json" -o -name "*metrics*.json" 2>/dev/null | head -1)
[[ -n "$EVAL_REPORT" ]] || { echo "no eval report"; exit 1; }
python -c "
import json
r = json.load(open('$EVAL_REPORT'))
# vlm_evaluator 报 metrics dict，至少含 narrative_quality / schema_compliance / anomaly_recall 之一
metrics = r.get('metrics', r)
print(f'OK eval report at $EVAL_REPORT keys: {list(metrics.keys())[:10]}')
assert len(metrics) > 0
"
```

Pass: 1 份 eval report 文件存在 + metrics 非空。
Finding 触发：smoke_small 的 train→eval_fp 链失败 / report 缺失 / metrics 全 0。

- [ ] **B1.5: pet-quantize 路径 — smoke_small 默认走 vlm_rkllm_w4a16，无 SDK 应 skip-with-warn**

> smoke_small.yaml 的 quantize stage 用 `converter/vlm_rkllm_w4a16`。无 SDK 时应触发 PET_ALLOW_MISSING_SDK skip 路径。如要测 noop 替换，用 `recipes/converter/noop_converter.yaml` override。

```bash
cd /workspace/pet-infra
# 法一：smoke_small 链中 quantize stage 自然触发 skip-with-warn
grep -i "missing.*SDK\|skip\|rkllm\|rknn" /tmp/sft.log | tail -20

# 法二：用 noop_converter 替换 quantize stage 重跑
pet run recipes/smoke_small.yaml \
  defaults='[_base/smoke_base, trainer/llamafactory_sft, evaluator/vlm_evaluator, dataset/vlm_calibration_subset, converter/noop_converter, ota/local_backend]' \
  stages.train.config_path=configs/eco/sft_train.yaml \
  2>&1 | tee /tmp/quantize-noop.log
echo "exit code: $?"
```

Pass: 法一 log 含 SDK warn，或法二 exit 0 + noop converter 产出占位 artifact。
Finding 触发：exit code != 0 / Phase failed at quantize stage（应当 graceful skip）。
Risk refs: spec 4.3 — Q4 import 守卫缺失则 STRUCTURAL。

- [ ] **B1.6: pet-ota local backend release — smoke_small deploy stage**

> smoke_small.yaml 的 deploy stage 已用 `ota/local_backend`，B1.3 跑完链时已发布过。这里 verify。

```bash
cd /workspace/pet-infra
# verify deploy stage 产出了 release 记录
RELEASE_DIR=$(find /workspace -path "*/releases*" -type d 2>/dev/null | head -1)
[[ -n "$RELEASE_DIR" ]] || { echo "no release dir"; exit 1; }
ls "$RELEASE_DIR"

# verify via real API
python -c "
from pet_ota.backend.local import LocalBackend
# LocalBackend 接受 release storage path; 看 backend/local.py 真实 ctor
b = LocalBackend(release_dir='$RELEASE_DIR')
# 列 release（看 backend.local 实际 API；read 一下 src/pet_ota/backend/local.py）
print(f'OK LocalBackend init at {RELEASE_DIR}')
"
```

Pass: release dir 存在 + LocalBackend 可初始化。
Finding 触发：deploy stage 失败 / LocalBackend ctor 签名与 plan 不符（plan 阶段未读全 src，rental 时 verify）。

> Plan-time TODO: rental 上需 `head -50 /workspace/pet-ota/src/pet_ota/backend/local.py` verify `LocalBackend.__init__` 签名再调整命令。

- [ ] **B1.7: pet-infra replay — `pet run --replay <card_id>`**

> 真实 CLI: `pet run --replay <card_id>` (不是独立的 `pet replay`)。先取 B1.3 modelcard 的 card_id。

```bash
cd /workspace/pet-infra
# 取最新 modelcard 的 card_id
MC_FILE=$(ls -t /workspace/runs/vision-sft/modelcard*.yaml | head -1)
CARD_ID=$(python -c "import yaml; print(yaml.safe_load(open('$MC_FILE'))['recipe_id'])")
echo "replaying card_id=$CARD_ID"

# 先 dry-run 验证 replay path（不重训）
pet run --replay "$CARD_ID" --dry-run 2>&1 | tee /tmp/replay-dryrun.log
grep -i "git.*drift\|sha.*match\|replay complete" /tmp/replay-dryrun.log
```

Pass: dry-run exit 0 + log 含 "replay complete" 或显示 resolved config。
Finding 触发：dry-run 报 git_shas drift（如本身 commit 没动则是 bug）/ replay 失败。
Risk refs: I2 北极星 Comparability — replay 不一致即 STRUCTURAL。

- [ ] **B1.8: append Phase 1 段落到 report.md + commit + push**

## Phase 2 — Per-Repo Capability Matrix

> 9 仓子矩阵；按 spec §3.x；6/9 必须走完才算 Phase 2 OK（含 pet-train + pet-eval + pet-infra 三核心）。
> 每个 capability 按 spec 表格 pass 判据执行；fail 即写 finding。

### B2.1 pet-schema (4 items: S1-S4)

- [ ] S1 — ExperimentRecipe / ModelCard / RecipeStage 模型 validate
- [ ] S2 — SFTSample/ShareGPTSFTSample/DPOSample（复用 B1.2 产物）
- [ ] S3 — SourceType 8 枚举各走一遍
- [ ] S4 — schema 破坏性 detection（CI guard 模拟改字段）
- [ ] commit + push

### B2.2 pet-data (5 items: D1-D5)

- [ ] D1 — 7 选 4 source（local_dir/youtube/oxford_pet/community）各塞 5-8
- [ ] D2 — dedup（故意塞 2 张同图）
- [ ] D3 — 弱监督打标
- [ ] D4 — video frame extract（4 段 video）
- [ ] D5 — pet-data 自定义 migration 幂等
- [ ] commit + push

### B2.3 pet-annotation (5 items: A1-A5)

- [ ] A1 — Sonnet VLM label（复用 B1.2）
- [ ] A2 — 4 范式表（llm 已有；classifier+rule+human stub 各 INSERT）
- [ ] A3 — DPO pair 生成 5 对（复用 B1.2）
- [ ] A4 — LLaMA-Factory exporter validate
- [ ] A5 — 人工审核 stub
- [ ] commit + push

### B2.4 pet-train (7 items: T1-T8 minus T5)

- [ ] T1 — Qwen2-VL-2B SFT + LoRA（复用 B1.3）
- [ ] T2 — Qwen2-VL-2B 全参 SFT（无 LoRA） — **OOM 时按 §4.6 决策合成**
- [ ] T3 — DPO 训练（用 5 对 + T1 checkpoint）
- [ ] T4 — Audio CNN zero-shot inference（PANNs MobileNetV2 + AUDIOSET_CLASS_MAP，16 段 audio → 5 类 distribution）
- [ ] T6 — tiny_test plugin
- [ ] T7 — from_params 全链（改 LR）
- [ ] T8 — LLaMA-Factory vendor 隔离 grep
- [ ] commit + push

### B2.5 pet-eval (5 items: E1-E5)

- [ ] E1 — vision evaluator（复用 B1.4）
- [ ] E2 — audio evaluator（zero-shot path，4 段 holdout audio）
- [ ] E3 — 3 fusion plugin via single shared config + variations sweep
- [ ] E4 — metric registry 扩展（dummy_metric plugin）
- [ ] E5 — recipe 切 plugin 不改代码
- [ ] commit + push

### B2.6 pet-quantize (4 items: Q1-Q4)

- [ ] Q1 — NoopConverter（复用 B1.5）
- [ ] Q2 — CONVERTERS registry（dummy plugin）
- [ ] Q3 — quantize_validate workflow 无设备
- [ ] Q4 — RKLLM/RKNN import 守卫
- [ ] commit + push

### B2.7 pet-ota (6 items: O1-O6)

- [ ] O1 — local backend（复用 B1.6）
- [ ] O2 — s3 backend（moto mock）
- [ ] O3 — http backend（in-process httpd）
- [ ] O4 — canary rollout 0→25→50→100
- [ ] O5 — rollback v2→v1
- [ ] O6 — gate_status check（gate=fail 拒绝）
- [ ] commit + push

### B2.8 pet-infra (7 items: I1-I7)

- [ ] I1 — Storage 3 后端 round-trip
- [ ] I2 — replay（复用 B1.7）
- [ ] I3 — variations launcher
- [ ] I4 — sweep + sweep_preflight
- [ ] I5 — ClearML 2 mode（offline + saas-fail with fallback_null）
- [ ] I6 — entry-point discover_plugins required 缺失
- [ ] I7 — compose_recipe 循环 defaults
- [ ] commit + push

### B2.9 pet-id (3 items: ID1-ID3)

- [ ] ID1 — 独立 CLI（zero pet-* import grep）
- [ ] ID2 — 一次端到端
- [ ] ID3 — make test
- [ ] commit + push

### B2.10 — Phase 2 retro 段落到 report.md

## Phase 3 — Cross-Cutting Concerns

- [ ] **B3.X1** — recipe 切 evaluator plugin（复用 B2.5 E3 + Hydra override 改权重）
- [ ] **B3.X2** — 多 modality 端到端（同次 `pet run` 跑 vision + audio + fusion）
- [ ] **B3.X3** — replay 跨 commit 漂移（modelcard 在另一 commit 上 replay）
- [ ] **B3.X4** — variations launcher 多 plugin 比较
- [ ] **B3.X5** — sweep + ClearML offline 集成
- [ ] **B3.X6** — experiment_logger 故障注入（saas + 断网 + fallback_null）
- [ ] **B3.X7** — storage 后端切换不改 plugin
- [ ] **B3.X8** — gate 拦截 OTA 发布
- [ ] **B3.X9** — Phase 3 retro 段落

> **范围决策**：如时间紧，至少完成 X1 + X2 + X7（critical path 上的解耦验证）；其他可降级 deferred。

---

# Part C — Rental 收尾

## Task C1: 北极星 4 维自评 + 生态指标核对

- [ ] **C1.1: 4 维各打分（1-5）+ 证据列表**

按 spec §1.3 不变量 #9 + PHASE_DOD §5：
- Pluggability：从 B2.4 T4 (新 modality) + B2.7 O1-O3 (3 backend) + B3.X7 (storage 切换) 取证
- Flexibility：从 B2.5 E5 + B3.X1 (Hydra override) + B2.8 I3 (variations) 取证
- Extensibility：从 B2.5 E4 + B2.6 Q2 + B2.8 I6 (entry-point) 取证
- Comparability：从 B2.5 E1+E2 (per-modal 同格式) + B2.8 I2 (replay) 取证

任一维 < 3 → trigger rework Phase。

- [ ] **C1.2: PHASE_DOD §1-§5 五大项核对**

按 spec §5.1 节"生态指标核对"。

- [ ] **C1.3: 写入 report.md "北极星 4 维自评"段落**

## Task C2: 写 retro 段落 + matrix 2026.11 草稿

- [ ] **C2.1: What worked / What didn't / 改进建议**

至少 5 条 worked + 5 条 didn't + 5 条 improvements。

- [ ] **C2.2: matrix 2026.11 草稿**

如本次 rental 触发 STRUCTURAL fix 导致版本 bump，写一个 `compatibility_matrix.yaml` 新行草稿，待用户 approve 后 PR。

## Task C3: B-batch fix 按主题分组开 PR

- [ ] **C3.1: 列出所有 B-batch commits**

```bash
git log --oneline feature/eco-validation-design-2026-04-25 ^dev | grep -i "fix"
```

- [ ] **C3.2: 按主题分组 → 每组开一个 feature/* PR**

举例分组：
- `fix/eco-pet-train-dataloader-*`
- `fix/eco-pet-eval-fusion-config-*`
- `fix/eco-pet-data-dedup-*`

每组：cherry-pick 相关 commit 到新分支 → push → `gh pr create --base dev`。

- [ ] **C3.3: critic 复审决策日志**

调 critic agent 复审 report.md 的"决策日志"段落，找潜在判断失误。

```python
# 调用 critic 二次意见
# Agent({subagent_type: "critic", prompt: "复审 report.md 决策日志，找判断失误..."})
```

## Task C4: 通知用户 + 关闭 rental

- [ ] **C4.1: report.md 元信息填充结束时间 / 总成本 / 总耗时**
- [ ] **C4.2: README.md 更新状态 → COMPLETED**
- [ ] **C4.3: 推所有 commit + 关闭 rental（用户操作）**

---

# 跨 Plan 通用约束

## 决策合成（rental 上每次分叉点）

按 spec §4.6：
1. 列 ≥ 2 候选
2. 4 条硬否决检查
3. 选择 + 1 句话写 report.md "决策日志"

## Find→Fix→Retest 闭环

按 spec §4.2 8 步流程；每个 finding 一个 .md 文件；retest 必须有命令输出 evidence。

## Skill 调用清单（按 Part / Task 索引）

| Skill | 用在 | 触发条件 |
|---|---|---|
| `superpowers:test-driven-development` | Part A 全部 task | 写 script 时 |
| `superpowers:systematic-debugging` | Part B 任何 finding | 第 3 步根因 |
| `superpowers:verification-before-completion` | Part B 任何 step / finding 第 8 步 | 关闭前 |
| `superpowers:requesting-code-review` | Part C C3 | 开 PR 前 |
| `superpowers:receiving-code-review` | Part C C3 | 收 critic / 用户 PR 反馈 |
| `superpowers:finishing-a-development-branch` | Part C C3 | 批量 PR |
| `critic` agent | C3.3 + 任何决策两难 | 二意见 |
| `tracer` agent | finding 跨仓 bug | 根因链 |
| `document-specialist` agent | A1 step 3（Anthropic 文档） | 外部文档 |

---

**EOF**
