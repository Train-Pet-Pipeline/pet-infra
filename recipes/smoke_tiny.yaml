defaults:
  - _base/smoke_base
  - trainer/tiny_test
  - converter/noop_converter
  - ota/local_backend

recipe_id: smoke_tiny
description: "PR-CI smoke; tiny trainer + noop converter + local OTA"
scope: single_repo
schema_version: "2.0.0"
smoke_tier: tiny

# Override deploy.depends_on: tiny skips eval_quant, so deploy follows quantize directly
stages:
  deploy:
    depends_on: [quantize]

produces:
  - smoke_tiny_edge_artifact
